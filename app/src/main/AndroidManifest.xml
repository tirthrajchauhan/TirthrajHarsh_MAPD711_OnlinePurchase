<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tirthraj.tirthrajharsh_mapd711_onlinepurchase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity" />
        <activity android:name=".PlaceOrderActivity" />
        <activity android:name=".CSRSignupActivity" />
        <activity android:name=".editOrderActivity" />
        <activity android:name=".NewShoesActivity" />
        <activity android:name=".CSRwelcomeActivity" />
        <activity android:name=".userWelcomeActivity"></activity>
    </application>

</manifest>